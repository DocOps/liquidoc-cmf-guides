include::{topic_page_meta}[tags="build-config-file"]

LiquiDoc configuration files are formatted in YAML.
They represent a sequential procedure, with each item in a YAML array representing a step or substep of the build.

Let's look at a very simple LD configuration.

[source,yaml]
----

----
